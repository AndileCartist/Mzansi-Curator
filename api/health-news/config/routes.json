{
  "routes": [
    {
      "method": "GET",
      "path": "/health-news",
      "handler": "health-news.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-news/count",
      "handler": "health-news.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-news/:id",
      "handler": "health-news.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/health-news",
      "handler": "health-news.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/health-news/:id",
      "handler": "health-news.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/health-news/:id",
      "handler": "health-news.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
